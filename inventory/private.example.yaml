schema: heterogeneous-compute-fabric/private-operations-v1

# Copy this structure into a separately access-controlled private repository.
# Do not commit a populated copy here. Never place raw secrets in this file.
nodes:
  - node_id: compute-01
    hostname: REPLACE_IN_PRIVATE_REPOSITORY
    private_network_name: REPLACE_IN_PRIVATE_REPOSITORY
    ssh_user: REPLACE_IN_PRIVATE_REPOSITORY
    credential_references:
      - REPLACE_WITH_PASSWORD_MANAGER_REFERENCE
    owners: []
    recovery_locations: []
    service_inventory: []
    last_connectivity_check: null

